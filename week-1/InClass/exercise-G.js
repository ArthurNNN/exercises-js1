const percentageStudents = 30 *100 / 34;
const percentageMentors = 4 * 100 / 34;
const roundPS = Math.round(percentageStudents);
const roundPM = Math.round(percentageMentors);
console.log("Percentage students: " + roundPS +"%");
console.log("Percentage mentors: " + roundPM + "%");